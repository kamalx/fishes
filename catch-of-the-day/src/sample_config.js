// This is a sample configuration file for your app's
// firebase database connection.
// Replace appropriate values for the given keys that
// you'll find from your firebase account into the JS
// object below and rename this file to `config.js`
// and everything else should work out of the box.
export const config = {
    "apiKey": {YOUR API KEY FROM FIREBASE ACCOUNT},
    "authDomain": {YOUR AUTH DOMAIN FROM FIREBASE ACCOUNT},
    "databaseURL": {YOUR DATABASE URL FROM FIREBASE ACCOUNT}
};
